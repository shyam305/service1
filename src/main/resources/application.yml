eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
server:
  port: 9191
spring:
  application:
    name: SERVICE-ONE

service2:
  name: SERVICE-TWO
service3:
  name: SERVICE-THREE